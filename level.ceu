;;;
type Tile = [pos:Point, path:_(char*)] + <
    Background = ()
    Liquid     = ()
    Hotspot    = ()
    Ground     = ()
>
var tiles = ...
;;;

task Level () {
    val pingus = tasks()
    every 1:s {
        println(`:number ceu_mem->pingus.Dyn->Bcast.Tasks.dyns.its`)
        spawn in pingus, Pingu([0,200])
    }
}
