;;;
type Tile = [pos:Point, path:_(char*)] + <
    Background = ()
    Liquid     = ()
    Hotspot    = ()
    Ground     = ()
>
var tiles = ...
;;;

task Level () :awakes {
    var pingus = coroutines()
    every 1s {
        println(`:number ceu_mem->pingus.Dyn->Bcast.Coros.cur`)
        spawn in pingus, Pingu([0,200])
    }
}
